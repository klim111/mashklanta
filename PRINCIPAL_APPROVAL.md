# שלב האישור העקרוני — איסוף ושמירת פרטי הבקשה

איסוף מלא של פרטי הבקשה לאישור עקרוני, בצד הלקוח ובצד היועץ, עם שמירה אוטומטית
לבסיס הנתונים, סנכרון מול פרופיל הלקוח ודוח מסכם להורדה כ‑PDF.

## מסכים

| נתיב | למי | תיאור |
| --- | --- | --- |
| **שלב 2 «אישור עקרוני»** בתהליך תכנון המשכנתא | לקוח | הטופס משובץ בראש השלב, מעל תיבות הגשת הבקשה לבנק |
| `/principal-approval` | לקוח | אותו טופס כמסך עצמאי (קישור מהדאשבורד) |
| `/advisor-dashboard/client/[id]/principal-approval` | יועץ | תיק האישור העקרוני של לקוח מסוים |

הטופס מוצג בשלב עצמו דרך `<PrincipalApproval embedded />` בתוך
`src/components/plan/stages/PreApprovalStage.tsx`: איסוף פרטי הבקשה קודם לפנייה
לבנק, ולכן הוא פותח את השלב, והתיבות שמתחתיו מלוות את ההגשה עצמה. נעילת
«תצוגה מקדימה» של `PlanWorkspace` חלה עליו כמו על שאר תוכן השלב.

הפרמטר `[id]` הוא `Client.id` — אותה רשומת ליווי שבה משתמשים שאר מסכי היועץ,
ולא טבלת שיוך נפרדת.

## מה נאסף

* **לווים** — שם פרטי ומשפחה, ת"ז, תאריך לידה, תאריך הנפקת תעודה ותוקפה, אזרחות
  זרה (וכן באילו מדינות), מגדר, טלפון, אימייל, מצב תעסוקתי, מצב משפחתי, מספר
  ילדים מתחת לגיל 21 וגיל כל ילד, חופשת לידה, עיר וכתובת מגורים, מיקוד, השכלה,
  קרבה לאיש ציבור, והקשר בין הלווים (ברמת התיק).
* **הכנסות** — לכל לווה, כמה מקורות הכנסה: סוג ההכנסה (שכיר / עצמאי / פנסיה /
  מזונות / מלגה / שכירות), סכום חודשי, מעסיק, תפקיד, תאריך תחילת העסקה, וכתובת
  ומיקוד מקום התעסוקה. **כאשר הוותק אצל המעסיק קטן משנה** נפתח אוטומטית בלוק
  חובה של מקום עבודה קודם עם כל פרטי התעסוקה.
* **חשבונות בנק** — בנק, סניף (מתוך קטלוג הסניפים), מספר חשבון, ושיוך החשבון
  לאחד או יותר מהלווים/ערבים. ניתן להוסיף חשבונות ללא הגבלה.
* **פרטי ההלוואה והנכס** — בעוד כמה זמן נדרשת ההלוואה, זכאות למשכנתא וסכומה,
  סך ההון העצמי, מחיר הנכס, כתובת ומיקוד הנכס, והאם הדירה נקנתה במקום התעסוקה
  (עם השוואה חיה בין עיר הנכס לערי מקומות התעסוקה שהוזנו).
* **מקורות מימון** — מכירת דירה, הלוואות אחרות, הון עצמי מתנה ממשפחה והון עצמי
  מחסכונות. **סך מקורות המימון נבדק בזמן אמת מול ההון העצמי שהוזן** ומוצג פער
  מדויק כשאין התאמה.
* **ערבים** — כל פרטי הלווה, בתוספת הקרבה ללווה ועבור מי ניתנת הערבות.
* **תיק המסמכים** — רשימת המסמכים נבנית לפי אופן ההעסקה של כל לווה וערב, מתוך
  קטלוג המסמכים הקיים (`client-process.ts`, `mortgage-plan.ts`) ולא מרשימה כפולה.
* **אישור עקרוני לפי בנק** — לכל בנק שאליו הוגשה הבקשה: תאריך הגשה, הבנקאי
  המטפל, האם התקבל אישור, תאריך האישור, הסכום שאושר, והריביות שהבנק נקב לכל
  מסלול בכל אחד משלושת הסלים האחידים.

### תוקף האישור והריביות

לא נשאלת שאלה על תוקף. שני התאריכים נגזרים מתאריך קבלת האישור:

* **תוקף האישור** — 3 חודשים מיום הקבלה.
* **תוקף הריביות שבאישור** — 24 ימים מיום הקבלה.

שניהם מוצגים עם מספר הימים שנותרו, ומסומנים באדום כשפג תוקפם. הקבועים
(`APPROVAL_VALIDITY_MONTHS`, `APPROVAL_RATES_VALIDITY_DAYS`) מוגדרים פעם אחת
ב-`schema.ts`.

### הקשר לשלבים שאחרי

השלב עצמו (`PreApprovalStage`) הוא נקודת החיבור בלבד: הוא מתרגם את האישורים
שנאספו לנתוני `PlanData.APPLICATIONS` שעליהם נשענים תנאי סגירת השלב והשלבים
שאחריו. התוכנית נושאת בנק אחד, והבנק שממשיך הוא **הראשון שסומן כמאושר** —
מוצג במפורש במסך כדי שלא תהיה בחירה נסתרת. שורות הסלים שומרות על הקישור
לתמהיל שכבר נשמר מהן (`mixKey` / `mixRecordId`).

## שמירה אוטומטית

כל שדה נשמר בנפרד דרך `PATCH /api/principal-approval/case/[caseId]/field`
בהשהיה של חצי שנייה מרגע ההקלדה. הערך נכתב לטבלת `CaseField` יחד עם מקור ההזנה
(לקוח / יועץ / פרופיל) ומזהה המשתמש ששמר. יציאה מהדף וכניסה מחדש טוענות את
הנתונים מבסיס הנתונים — אין הסתמכות על אחסון מקומי בדפדפן.

## פרופיל הלקוח וסתירות

הפרופיל **אינו נשמר כאן**. הוא נשאר במקום שבו כל המערכת כבר מחזיקה אותו —
`User.profileJson`, שנקרא ונכתב דרך `src/lib/client-profile.ts`. המודול
`src/lib/principal-approval/profile.ts` רק מתרגם בין הפרופיל לשדות הטופס.

יש שני סוגי קישור:

1. **שדות דו‑כיווניים** — אותה עובדה בדיוק בשני הצדדים, ולכן פער ביניהם הוא
   סתירה אמיתית: `equity` ↔ ההון העצמי, ו‑`employmentType` /
   `partnerEmploymentType` ↔ המצב התעסוקתי של הלווה ושל בן/בת הזוג.
   כאן נפתחת `FieldConflict`, המשתמש רואה את **שני הערכים זה לצד זה**, והערך
   שנבחר נכתב גם לתיק וגם לפרופיל — כך שכל מסך אחר שמושך מהפרופיל מתעדכן.
2. **שדות נגזרים** — ערכים שהתיק מחשב מכמה רשומות (סך ההכנסה לכל לווה, הרכב
   הלווים, הבנק הראשי). התיק הוא המקור המפורט יותר, ולכן הם נדחפים לפרופיל
   בשקט: בקשה מהמשתמש "לבחור" בין סכום לבין השורות שממנו הוא חושב אינה אומרת
   דבר.

מיפוי המצב התעסוקתי מאבד מידע בכיוון אחד (הפרופיל מכיר שכיר/עצמאי בלבד, הטופס
אוסף חמישה מצבים), ולכן ערך שאין לו מקבילה בפרופיל אינו מייצר סתירת שווא.

בנוסף, בפתיחת תיק ממולאים כברירת מחדל — ללא סתירות — השם והאימייל מחשבון
המשתמש, ופרטי הקשר והנכס מרשומת ה‑`Client` של היועץ כשהתיק נפתח מלוח הבקרה שלו.

## הרשאות יועץ מול לקוח

* היועץ רואה את התיק המלא, ויכול למלא שדות ריקים ולערוך שדות שהוא עצמו הזין.
* שדות שהזין **הלקוח** מוצגים ליועץ לקריאה בלבד (עם מנעול והסבר).
* הלקוח יכול לבחור יועץ ולתת לו הרשאת עריכה (`ApprovalAccess.canEdit`) —
  ואז היועץ יכול לערוך גם את נתוני הלקוח. הרשאה ניתנת רק ליועץ שמלווה את
  הלקוח בפועל (קיימת רשומת `Client` ביניהם).
* אצל הלקוח, שדות שהזין היועץ מסומנים בתגית **"הוזן על ידי היועץ"**.

הכלל מרוכז ב‑`src/lib/principal-approval/access.ts` ונאכף גם בשרת
(`saveField` / `removeEntity`), לא רק ב‑UI.

## אימות נתונים

`src/lib/principal-approval/validation.ts` מריץ את אותם כללים בדפדפן (בזמן
ההקלדה) ובשרת (לפני השמירה): ספרת ביקורת של תעודת זהות, מספרי טלפון ישראליים,
מיקוד בן 7 ספרות, אימייל, תאריכים (כולל איסור תאריך עתידי היכן שרלוונטי),
מספרי חשבון, טווחים מספריים ובחירה מתוך רשימה סגורה.

שדות מספריים מקבלים **פסיקים בין אלפים תוך כדי הקלדה**, עם שמירה על מיקום
הסמן. היכולת נוספה לשדה המספר המשותף של המערכת
(`src/components/ui/numeric-input.tsx`) ולא לעותק נפרד, כך שהיא חלה על כל
המסכים. הקיבוץ פעיל במצב `integer` (סכומים בשקלים) בלבד: במצב עשרוני הפסיק
משמש כמפריד עשרוני (ראה `sanitizeDecimalInput`), ושם קיבוץ אלפים היה יוצר
דו‑משמעות.

## הצפנה של נתונים רגישים

שדות רגישים — הכנסות, הון עצמי, סכומי מקורות מימון, מחיר הנכס, סכום הזכאות,
מספרי חשבון ותעודות זהות — נשמרים **מוצפנים** בעמודה `CaseField.valueEnc`
דרך `src/lib/crypto.ts`, באותה מדיניות שנוהגת בשדות הכספיים של מודל `Client`.
ההצפנה קשורה לשורה עצמה (AAD הכולל את מזהה התיק, הרשומה והשדה), כך שאי אפשר
להעביר טקסט מוצפן בין שדות. שדה מסומן כרגיש באמצעות `sensitive: true`
ב‑`src/lib/principal-approval/schema.ts`.

נדרש `FIELD_ENCRYPTION_KEY` בסביבה — אותו מפתח שכבר משמש את שאר השדות
המוצפנים.

## קטלוג סניפי הבנקים

רשימת הבנקים (קוד ושם) מוגדרת ב‑`src/lib/banks/banks.ts`. רשימת **הסניפים**
נשמרת בטבלת `BankBranch` ומיובאת מהמאגר הציבורי הרשמי:

```bash
npm run sync:bank-branches                      # מהמאגר הציבורי (data.gov.il)
npm run sync:bank-branches -- --file ./branches.csv   # מקובץ מקומי (CSV/JSON)
```

ניתן להצביע על מקור אחר באמצעות `BANK_BRANCHES_URL`. כל עוד הקטלוג של בנק
מסוים לא יובא, שדה הסניף יורד אוטומטית להזנה ידנית של מספר הסניף עם הודעה
מתאימה — כדי לא לחסום את הלקוח.

## הדוח

לשונית **"דוח מסכם"** מציגה דוח מקצועי של כל הנתונים, כולל נתוני כותרת (מחיר
הנכס, הון עצמי, סכום מבוקש ושיעור מימון), ומאפשרת הורדה כ‑PDF דרך חלון ההדפסה
(גיליון הדפסה ייעודי ב‑`src/app/globals.css`).

בראש הדוח, לפני שאר הפרטים, מופיע **סיכום לפי בנקים**: לשונית לכל בנק שאליו
הוגשה הבקשה, ובה תוקף האישור, תוקף הריביות, הסכום שאושר והריביות של אותו בנק
לשלושת הסלים האחידים. מתחתיה **טבלת ריכוז ריביות לכל מסלול** — הריבית של כל
בנק זה לצד זה, עם סימון הבנק הזול ביותר לכל מסלול. בהדפסה מודפסים כל הבנקים,
ולא רק הלשונית הפתוחה.

## ניווט

הסעיפים מסודרים בלשוניות (לווים והכנסות · חשבונות בנק · הלוואה ונכס · מקורות
מימון · ערבים · תיק המסמכים · אישור עקרוני לפי בנק · דוח מסכם), עם חיצי מעבר
לסעיף הקודם והבא בתחתית המסך. הלווים והערבים עצמם מסודרים בלשונית לכל אדם,
ושם הלשונית הופך לשמו הפרטי ושם משפחתו ברגע שהוזנו.

## בדיקות

```bash
npm test                                   # כולל 39 בדיקות יחידה לשלב זה

# בדיקות אינטגרציה מול בסיס נתונים אמיתי (מריצות כתיבה — הפעלה מפורשת):
npx prisma migrate deploy
PRINCIPAL_APPROVAL_DB_TESTS=1 FIELD_ENCRYPTION_KEY=<base64-32-bytes> \
  npx vitest run __tests__/principal-approval.integration.test.ts
```

בדיקות האינטגרציה מכסות מילוי מראש מהפרופיל, זיהוי סתירה וכתיבה חזרה לפרופיל,
הצפנה בפועל בעמודה, הרשאות היועץ, והוספה והסרה של רשומות.
