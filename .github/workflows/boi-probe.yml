name: boi-probe
on:
  push:
    branches: [claude/boi-mortgage-dashboard-522yzq]
    paths: ['.github/workflows/boi-probe.yml', 'scripts/boi-probe.sh']
jobs:
  probe:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: bash scripts/boi-probe.sh
