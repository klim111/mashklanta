# ==============================================
# REQUIRED ENVIRONMENT VARIABLES
# ==============================================

# Database Configuration (REQUIRED)
# PostgreSQL connection string from Neon or other provider
DATABASE_URL=postgresql://username:password@host/database?sslmode=require

# NextAuth Configuration (REQUIRED)
# Generate with: openssl rand -base64 32
NEXTAUTH_SECRET=your-secret-key-here-generate-with-openssl

# Your production domain (without trailing slash)
NEXTAUTH_URL=https://your-domain.vercel.app

# ==============================================
# OPTIONAL ENVIRONMENT VARIABLES
# ==============================================

# Redis Configuration (Optional - for caching)
REDIS_URL=redis://default:password@host:port

# AWS S3 Configuration (Optional - for file uploads)
S3_REGION=us-east-1
S3_BUCKET=your-bucket-name
AWS_ACCESS_KEY_ID=your-access-key
AWS_SECRET_ACCESS_KEY=your-secret-key

# Bank of Israel API (Optional - for exchange rates)
BOI_RATES_URL=https://api.boi.org.il/rates
RATES_CACHE_TTL=900

# Node Environment (automatically set by Vercel)
# NODE_ENV=production